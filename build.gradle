allprojects {

    group = 'org.box2d.jni'
    version = "${jjVersion}"
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'java-library'
    apply plugin: 'application'
    apply plugin: 'maven-publish'
}