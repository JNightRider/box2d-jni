

+ API changes:
    + Removed `MathFunctions.b2OffsetWorldTransform(...)`
    + Removed `MathFunctions.nb2OffsetWorldTransform(...)`
    + Removed `b2PlaneSolverResult.traslation(...)` (setter/getter)
    + Removed `b2WorldDef.enableContactSoftening(...)` (setter/getter)
    + Removed `b2Profile.applyRestitution()` (setter/getter)

+ Update libfii v3.8.0
+ Update box2d b22026-08-27
+ Fix `b2SolvePlanes()` to accept a buffer

+ Added 28 public methods:
    + `ArenaAlloc.malloc(int)`
    + `ArenaAlloc.mallocFloat(int)`
    + `ArenaAlloc.mallocInt(int)`
    + `ArenaAlloc.calloc(int)`
    + `ArenaAlloc.callocFloat(int)`
    + `ArenaAlloc.callocInt(int)`
    + `b2BodyDef.safetyFactor(...)` (setter/getter)
    + `b2ManifoldPoint.restitutionVelocity(...)` (setter/getter)
    + `b2PlaneSolverResult.delta(...)` (setter/getter)
    + `Types.b2DefaultMoverJointDef(...)`
    + `Types.b2DefaultPogoJointDef(...)`
    + `Box2d.b2CreatePogoJoint(...)`
    + `Box2d.b2PogoJoint_SetRestLength(...)`
    + `Box2d.b2PogoJoint_GetRestLength(...)`
    + `Box2d.b2PogoJoint_SetSpringHertz(...)`
    + `Box2d.b2PogoJoint_GetSpringHertz(...)`
    + `Box2d.b2PogoJoint_SetSpringDampingRatio(...)`
    + `Box2d.b2PogoJoint_GetSpringDampingRatio(...)`
    + `Box2d.b2PogoJoint_GetLength(...)`
    + `Box2d.b2PogoJoint_GetVelocity(...)`
    + `Box2d.b2PogoJoint_GetImpulse(...)`
    + `Box2d.b2CreateMoverJoint(...)`
    + `Box2d.b2MoverJoint_SetLinearVelocity(...)`
    + `Box2d.b2MoverJoint_GetLinearVelocity(...)`
    + `Box2d.b2MoverJoint_SetMaxVelocityForce(...)`
    + `Box2d.b2MoverJoint_GetMaxVelocityForce(...)`
    + `Box2d.b2World_GetStateHash(...)`
    + `Box2d.b2Body_GetSafetyFactor(...)`
    + `Box2d.b2Body_GetMinExtent(...)`
    + `Box2d.b2Body_GetMaxExtent(...)`
    + `Box2d.b2Body_GetMaxExtentOrigin(...)`

+ New featurese:
    + `LibCFloat.FLT_MAX`
    + `b2PreContinuousFcnI/b2PreContinuousFcn`
    + `b2JointType.b2_moverJoint`
    + `b2JointType.b2_pogoJoint`
    + `b2MoverJointDef`
    + `b2PogoJointDef`

+ refactoring
    + `b2PreSolveFcnTest`
    + `b2PreSolveFcnI/b2PreSolveFcn`
    + `Box2d.b2DestroyJoint( b2JointId jointId )`

+ Added 1 test:
    + `b2PreContinuousFcnTest`
